---
phase: 04-frontend-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/synthetic-dataset-generator/frontend/package.json
  - examples/synthetic-dataset-generator/frontend/vite.config.ts
  - examples/synthetic-dataset-generator/frontend/tsconfig.json
  - examples/synthetic-dataset-generator/frontend/tailwind.config.js
  - examples/synthetic-dataset-generator/frontend/postcss.config.js
  - examples/synthetic-dataset-generator/frontend/index.html
  - examples/synthetic-dataset-generator/frontend/src/main.tsx
  - examples/synthetic-dataset-generator/frontend/src/App.tsx
  - examples/synthetic-dataset-generator/frontend/src/index.css
  - examples/synthetic-dataset-generator/frontend/src/components/layout/Sidebar.tsx
  - examples/synthetic-dataset-generator/frontend/src/components/layout/Layout.tsx
  - examples/synthetic-dataset-generator/frontend/src/components/layout/MobileNav.tsx
autonomous: true
requirements: [UI-01, UI-02, UI-05]

must_haves:
  truths:
    - "Split-panel layout renders with sidebar (240px), center chat area, right schema/preview panels"
    - "Dark theme applied with #0f172a slate-900 background, #3b82f6 blue-500 primary, #f59e0b amber-500 secondary accents"
    - "Mobile breakpoint triggers tab navigation (Chat | Schema | Preview) with one panel visible at a time"
    - "Hamburger menu on mobile opens slide-in sidebar"
  artifacts:
    - path: "examples/synthetic-dataset-generator/frontend/package.json"
      provides: "Vite + React + Tailwind + shadcn/ui dependencies"
      contains: "vite"
    - path: "examples/synthetic-dataset-generator/frontend/src/App.tsx"
      provides: "Root layout component with split-panel structure"
      exports: ["App"]
    - path: "examples/synthetic-dataset-generator/frontend/src/components/layout/Layout.tsx"
      provides: "Main layout component with sidebar, chat, right panels"
      exports: ["Layout"]
    - path: "examples/synthetic-dataset-generator/frontend/src/index.css"
      provides: "Tailwind base styles and CSS custom properties for dark theme"
      contains: "--background"
  key_links:
    - from: "examples/synthetic-dataset-generator/frontend/src/main.tsx"
      to: "examples/synthetic-dataset-generator/frontend/src/App.tsx"
      via: "React DOM render"
      pattern: "createRoot.*App"
    - from: "examples/synthetic-dataset-generator/frontend/src/App.tsx"
      to: "examples/synthetic-dataset-generator/frontend/src/components/layout/Layout.tsx"
      via: "Layout component import"
      pattern: "import.*Layout"
---

<objective>
Create React frontend scaffold with Vite, Tailwind CSS, and split-panel layout matching the Pencil mockup.

Purpose: Establish project foundation and visual layout that all subsequent plans build upon.
Output: Runnable React app with dark theme, responsive split-panel layout, sidebar navigation, and mobile tab bar.
</objective>

<execution_context>
@/Users/feffendi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/feffendi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-frontend-ui/04-CONTEXT.md

Color scheme (CSS variables for easy theming):
All colors defined as HSL values in index.css. To change theme, edit one file.

Current palette (Tailwind Slate + Blue):
- --background: slate-900    --sidebar: slate-950
- --surface: slate-800       --input: slate-700
- --primary: blue-500        --accent: amber-500
- --foreground: slate-100    --muted-foreground: slate-400

Layout dimensions (from user decisions):
- Sidebar: 240px fixed
- Chat panel: 50% of remaining width
- Right panels: 50% of remaining width (40% schema, 60% preview)
- Mobile breakpoint: standard Tailwind lg (1024px)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Vite + React + Tailwind project scaffold</name>
  <files>
    examples/synthetic-dataset-generator/frontend/package.json
    examples/synthetic-dataset-generator/frontend/vite.config.ts
    examples/synthetic-dataset-generator/frontend/tsconfig.json
    examples/synthetic-dataset-generator/frontend/tailwind.config.js
    examples/synthetic-dataset-generator/frontend/postcss.config.js
    examples/synthetic-dataset-generator/frontend/index.html
    examples/synthetic-dataset-generator/frontend/src/main.tsx
    examples/synthetic-dataset-generator/frontend/src/vite-env.d.ts
  </files>
  <action>
Create the frontend directory and scaffold a Vite + React + TypeScript project:

1. Create `package.json` with dependencies:
   - react, react-dom (^18.2.0)
   - vite (^5.0.0), @vitejs/plugin-react (^4.2.0)
   - typescript, @types/react, @types/react-dom
   - tailwindcss (^3.4.0), postcss, autoprefixer
   - lucide-react (^0.400.0)
   - class-variance-authority, clsx, tailwind-merge (for shadcn/ui utils)
   - Scripts: dev, build, preview

2. Create `vite.config.ts` with React plugin, build output to `build/` directory (matches Frontend construct default).

3. Create `tsconfig.json` with strict mode, React JSX, path alias `@/` mapping to `./src/`.

4. Create `tailwind.config.js` with CSS variable references for easy theming:
   - Content paths: `./index.html`, `./src/**/*.{ts,tsx}`
   - Extended theme colors using CSS variables (defined in index.css):
     ```javascript
     colors: {
       background: 'hsl(var(--background))',
       sidebar: 'hsl(var(--sidebar))',
       surface: 'hsl(var(--surface))',
       input: 'hsl(var(--input))',
       primary: {
         DEFAULT: 'hsl(var(--primary))',
         foreground: 'hsl(var(--primary-foreground))',
       },
       accent: {
         DEFAULT: 'hsl(var(--accent))',
         foreground: 'hsl(var(--accent-foreground))',
       },
       muted: {
         DEFAULT: 'hsl(var(--muted))',
         foreground: 'hsl(var(--muted-foreground))',
       },
       foreground: 'hsl(var(--foreground))',
       border: 'hsl(var(--border))',
     }
     ```
   - Dark mode: class-based (darkMode: 'class')
   - This pattern allows theme changes by editing CSS only (no Tailwind rebuild)

5. Create `postcss.config.js` with tailwindcss and autoprefixer plugins.

6. Create `index.html` with:
   - Dark theme class on html element
   - Meta viewport for mobile
   - Font: Inter from Google Fonts CDN
   - Root div with id="root"
   - Script module pointing to /src/main.tsx

7. Create `src/main.tsx` with ReactDOM.createRoot rendering App component.

8. Create `src/vite-env.d.ts` with Vite client types reference.

Do NOT run npm install - the executor will handle dependencies during CDK build.
  </action>
  <verify>
    <automated>ls -la examples/synthetic-dataset-generator/frontend/package.json examples/synthetic-dataset-generator/frontend/vite.config.ts examples/synthetic-dataset-generator/frontend/src/main.tsx</automated>
  </verify>
  <done>
    - package.json exists with all required dependencies
    - vite.config.ts configures React plugin and build output
    - tailwind.config.js has dark theme colors from mockup
    - src/main.tsx renders App component
  </done>
</task>

<task type="auto">
  <name>Task 2: Create dark theme CSS and utility functions</name>
  <files>
    examples/synthetic-dataset-generator/frontend/src/index.css
    examples/synthetic-dataset-generator/frontend/src/lib/utils.ts
  </files>
  <action>
1. Create `src/index.css` with CSS variables as single source of truth:
   - Tailwind directives (@tailwind base, components, utilities)
   - CSS custom properties in :root using HSL format (for easy manipulation):
     ```css
     @layer base {
       :root {
         /* Dark theme - Tailwind Slate + Blue palette */
         --background: 222.2 47.4% 11.2%;      /* #0f172a slate-900 */
         --sidebar: 222.2 84% 4.9%;            /* #020617 slate-950 */
         --surface: 217.2 32.6% 17.5%;         /* #1e293b slate-800 */
         --input: 215 25% 27%;                 /* #334155 slate-700 */
         --border: 217.2 32.6% 17.5%;          /* #1e293b slate-800 */

         --foreground: 210 40% 98%;            /* #f1f5f9 slate-100 */
         --muted: 215 16.3% 46.9%;             /* #64748b slate-500 */
         --muted-foreground: 215 20.2% 65.1%;  /* #94a3b8 slate-400 */

         --primary: 217.2 91.2% 59.8%;         /* #3b82f6 blue-500 */
         --primary-foreground: 222.2 47.4% 11.2%;

         --accent: 38 92% 50%;                 /* #f59e0b amber-500 */
         --accent-foreground: 222.2 47.4% 11.2%;
       }
     }
     ```
   - Base styles: body with antialiased, bg-background text-foreground
   - Scrollbar styling for dark theme (optional polish)

   **To change theme later:** Just edit the HSL values above. No other files need changes.

2. Create `src/lib/utils.ts` with:
   - `cn()` function using clsx and tailwind-merge for conditional class merging
   - This is the standard shadcn/ui utility pattern

Example cn function:
```typescript
import { type ClassValue, clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```
  </action>
  <verify>
    <automated>ls examples/synthetic-dataset-generator/frontend/src/index.css examples/synthetic-dataset-generator/frontend/src/lib/utils.ts</automated>
  </verify>
  <done>
    - index.css has Tailwind directives and CSS custom properties
    - utils.ts exports cn() function for class merging
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Layout components with split-panel structure</name>
  <files>
    examples/synthetic-dataset-generator/frontend/src/components/layout/Layout.tsx
    examples/synthetic-dataset-generator/frontend/src/components/layout/Sidebar.tsx
    examples/synthetic-dataset-generator/frontend/src/components/layout/MobileNav.tsx
    examples/synthetic-dataset-generator/frontend/src/App.tsx
  </files>
  <action>
1. Create `src/components/layout/Sidebar.tsx`:
   - Fixed width 240px on desktop, slide-in drawer on mobile
   - Logo area: primary square icon + "DataSynth" text
   - Navigation items: "New Dataset" (primary highlight, active state), "History" (muted, disabled for v1)
   - Sign Out button at bottom (accent background (amber))
   - Props: `isOpen: boolean`, `onClose: () => void` for mobile drawer behavior
   - Use Lucide icons: Plus, FolderOpen, LogOut

2. Create `src/components/layout/MobileNav.tsx`:
   - Bottom tab bar for mobile (below lg breakpoint)
   - Three tabs: Chat (MessageSquare icon), Schema (Table2 icon), Preview (Eye icon)
   - Props: `activeTab: 'chat' | 'schema' | 'preview'`, `onTabChange: (tab) => void`
   - Active tab shows primary color, inactive shows muted

3. Create `src/components/layout/Layout.tsx`:
   - Main layout container with flex row
   - Desktop (lg+): Sidebar visible, three-column layout
     - Sidebar: w-60 (240px)
     - Chat panel: flex-1 (takes remaining 50%)
     - Right panels: w-[480px] or 50% max, split vertically
   - Mobile (<lg): Full-width single panel with bottom tabs
     - Only show active panel based on tab state
   - Right panel split: 40% schema (top), 60% preview (bottom)
   - Props: children slots for ChatPanel, SchemaPanel, PreviewPanel
   - Use useState for mobile sidebar open state and active tab

4. Create `src/App.tsx`:
   - Import and render Layout component
   - Placeholder content for each panel area (will be replaced in subsequent plans)
   - Placeholder: "Chat Panel", "Schema Panel", "Preview Panel" text in each area

Tailwind classes (colors from CSS variables - edit index.css to change theme):
- `bg-background` - main background (slate-900)
- `bg-sidebar` - sidebar (slate-950)
- `bg-surface` - cards, panels (slate-800)
- `bg-input` - input fields, table headers (slate-700)
- `text-foreground` - primary text (slate-100)
- `text-muted-foreground` - secondary text (slate-400)
- `bg-primary` / `text-primary` - active states, primary accents (blue-500)
- `bg-accent` - CTA buttons, highlights (amber-500)
  </action>
  <verify>
    <automated>ls examples/synthetic-dataset-generator/frontend/src/components/layout/Layout.tsx examples/synthetic-dataset-generator/frontend/src/components/layout/Sidebar.tsx examples/synthetic-dataset-generator/frontend/src/App.tsx</automated>
  </verify>
  <done>
    - Layout.tsx renders split-panel structure with sidebar, chat area, right panels
    - Sidebar.tsx has logo, navigation, sign out button
    - MobileNav.tsx has bottom tab bar with three tabs
    - App.tsx renders Layout with placeholder content
    - Responsive breakpoint at lg (1024px) switches between desktop and mobile layouts
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. All files exist in frontend directory
2. package.json has correct dependencies (react, vite, tailwind, lucide-react)
3. tailwind.config.js has custom dark theme colors
4. Layout components follow mockup structure
5. Mobile layout uses tab navigation pattern
</verification>

<success_criteria>
- Frontend directory exists with complete Vite + React + Tailwind scaffold
- Dark theme colors from Pencil mockup are configured in Tailwind
- Split-panel layout matches mockup: sidebar (240px), chat (50%), right panels (50%)
- Responsive design: mobile shows tabs + single panel, desktop shows all panels
- All TypeScript files compile without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-ui/04-01-SUMMARY.md`
</output>
