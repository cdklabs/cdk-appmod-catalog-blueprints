---
phase: 01-agent-infrastructure-script-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/synthetic-dataset-generator/app.ts
  - examples/synthetic-dataset-generator/cdk.json
  - examples/synthetic-dataset-generator/package.json
  - examples/synthetic-dataset-generator/tsconfig.json
autonomous: true
requirements:
  - INFRA-06

must_haves:
  truths:
    - "Example directory exists at examples/synthetic-dataset-generator/"
    - "CDK app entry point compiles without errors"
    - "npm install succeeds in the example directory"
  artifacts:
    - path: "examples/synthetic-dataset-generator/app.ts"
      provides: "CDK app entry point"
      contains: "SyntheticDatasetGeneratorStack"
    - path: "examples/synthetic-dataset-generator/cdk.json"
      provides: "CDK configuration"
      contains: "npx ts-node"
    - path: "examples/synthetic-dataset-generator/package.json"
      provides: "NPM package configuration"
      contains: "@cdklabs/cdk-appmod-catalog-blueprints"
    - path: "examples/synthetic-dataset-generator/tsconfig.json"
      provides: "TypeScript configuration"
      contains: "compilerOptions"
  key_links:
    - from: "examples/synthetic-dataset-generator/app.ts"
      to: "synthetic-dataset-generator-stack.ts"
      via: "import statement"
      pattern: "import.*SyntheticDatasetGeneratorStack"
---

<objective>
Create the CDK example application scaffolding for the synthetic dataset generator.

Purpose: Establish the example directory structure with all required configuration files so subsequent plans can add the stack and resources.
Output: Deployable CDK app skeleton with app.ts, cdk.json, package.json, tsconfig.json
</objective>

<execution_context>
@/Users/feffendi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/feffendi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-agent-infrastructure-script-generation/01-CONTEXT.md
@.planning/phases/01-agent-infrastructure-script-generation/01-RESEARCH.md

Reference existing examples for patterns:
@examples/rag-customer-support/app.ts
@examples/rag-customer-support/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create example directory and configuration files</name>
  <files>
    examples/synthetic-dataset-generator/cdk.json,
    examples/synthetic-dataset-generator/package.json,
    examples/synthetic-dataset-generator/tsconfig.json
  </files>
  <action>
Create the example directory at `examples/synthetic-dataset-generator/` and add the following files:

1. **cdk.json** - CDK configuration:
```json
{
  "app": "npx ts-node --prefer-ts-exts app.ts",
  "context": {
    "@aws-cdk/core:checkSecretUsage": true,
    "@aws-cdk/aws-iam:minimizePolicies": true
  }
}
```

2. **package.json** - NPM configuration (follow existing example patterns):
```json
{
  "name": "synthetic-dataset-generator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "cdk": "cdk",
    "deploy": "cdk deploy --require-approval never"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "aws-cdk": "^2.218.0",
    "ts-node": "^10.9.1",
    "typescript": "~5.7.2"
  },
  "dependencies": {
    "@cdklabs/cdk-appmod-catalog-blueprints": "^0.0.0",
    "aws-cdk-lib": "^2.218.0",
    "constructs": "^10.0.0",
    "source-map-support": "^0.5.21"
  }
}
```

3. **tsconfig.json** - TypeScript configuration:
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "lib": ["ES2020"],
    "declaration": true,
    "strict": true,
    "noImplicitAny": true,
    "strictNullChecks": true,
    "noImplicitThis": true,
    "alwaysStrict": true,
    "noUnusedLocals": false,
    "noUnusedParameters": false,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": false,
    "inlineSourceMap": true,
    "inlineSources": true,
    "experimentalDecorators": true,
    "strictPropertyInitialization": false,
    "outDir": "./dist",
    "rootDir": "."
  },
  "exclude": ["node_modules", "cdk.out", "dist"]
}
```
  </action>
  <verify>
    <automated>ls -la examples/synthetic-dataset-generator/ && cat examples/synthetic-dataset-generator/package.json | head -10</automated>
  </verify>
  <done>Configuration files exist with correct content matching repository patterns</done>
</task>

<task type="auto">
  <name>Task 2: Create CDK app entry point and resource directories</name>
  <files>
    examples/synthetic-dataset-generator/app.ts,
    examples/synthetic-dataset-generator/resources/.gitkeep
  </files>
  <action>
1. **app.ts** - CDK application entry point:
```typescript
#!/usr/bin/env node
import 'source-map-support/register';
import * as cdk from 'aws-cdk-lib';
import { SyntheticDatasetGeneratorStack } from './synthetic-dataset-generator-stack';

const app = new cdk.App();
new SyntheticDatasetGeneratorStack(app, 'SyntheticDatasetGeneratorStack', {
  env: {
    account: process.env.CDK_DEFAULT_ACCOUNT,
    region: process.env.CDK_DEFAULT_REGION,
  },
});
```

2. Create empty resource directories for subsequent plans:
   - `examples/synthetic-dataset-generator/resources/system-prompt/` (for conversation prompt)
   - `examples/synthetic-dataset-generator/resources/generation/` (for script generation prompt)
   - `examples/synthetic-dataset-generator/resources/tools/` (for generate_script tool)

Add `.gitkeep` files to each directory to ensure they're tracked.

Note: The SyntheticDatasetGeneratorStack import will show a TypeScript error until Plan 03 creates the stack file. This is expected - the file structure is being prepared.
  </action>
  <verify>
    <automated>cat examples/synthetic-dataset-generator/app.ts && ls -la examples/synthetic-dataset-generator/resources/</automated>
  </verify>
  <done>app.ts exists with correct imports; resources directories created with gitkeep files</done>
</task>

</tasks>

<verification>
1. Directory exists: `ls examples/synthetic-dataset-generator/`
2. All config files present: `ls examples/synthetic-dataset-generator/*.json`
3. app.ts compiles (will have import error until stack created - this is expected)
4. Resource directories ready: `ls examples/synthetic-dataset-generator/resources/`
</verification>

<success_criteria>
- [ ] examples/synthetic-dataset-generator/ directory exists
- [ ] cdk.json exists with correct CDK configuration
- [ ] package.json exists with correct dependencies including @cdklabs/cdk-appmod-catalog-blueprints
- [ ] tsconfig.json exists with ES2020 target and strict mode
- [ ] app.ts exists with SyntheticDatasetGeneratorStack import (import error expected until Plan 03)
- [ ] resources/system-prompt/, resources/generation/, resources/tools/ directories exist
</success_criteria>

<output>
After completion, create `.planning/phases/01-agent-infrastructure-script-generation/01-01-SUMMARY.md`
</output>
