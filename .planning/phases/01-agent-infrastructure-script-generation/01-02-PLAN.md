---
phase: 01-agent-infrastructure-script-generation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - examples/synthetic-dataset-generator/resources/system-prompt/conversation-prompt.txt
  - examples/synthetic-dataset-generator/resources/generation/script-generation-prompt.txt
  - examples/synthetic-dataset-generator/resources/tools/generate_script.py
autonomous: true
requirements:
  - CHAT-05
  - GEN-01
  - GEN-02
  - GEN-03
  - GEN-04

must_haves:
  truths:
    - "Conversation prompt defines adaptive requirement gathering behavior"
    - "Script generation prompt enforces DataGenerator class template"
    - "Generated scripts use pinned versions matching Lambda layer (pandas, numpy, faker)"
    - "Tool sanitizes user inputs before passing to BatchAgent"
    - "Tool returns structured response with script, summary, and error handling"
  artifacts:
    - path: "examples/synthetic-dataset-generator/resources/system-prompt/conversation-prompt.txt"
      provides: "InteractiveAgent conversation behavior"
      contains: "DataSynth"
    - path: "examples/synthetic-dataset-generator/resources/generation/script-generation-prompt.txt"
      provides: "BatchAgent script generation template enforcement"
      contains: "DataGenerator"
    - path: "examples/synthetic-dataset-generator/resources/tools/generate_script.py"
      provides: "Tool that invokes BatchAgent for script generation"
      contains: "@tool"
  key_links:
    - from: "examples/synthetic-dataset-generator/resources/tools/generate_script.py"
      to: "BatchAgent Lambda"
      via: "boto3 Lambda.invoke"
      pattern: "lambda_client.invoke"
    - from: "examples/synthetic-dataset-generator/resources/generation/script-generation-prompt.txt"
      to: "Generated Python scripts"
      via: "Template enforcement"
      pattern: "class DataGenerator"
---

<objective>
Create the agent resources: system prompts for both agents and the generate_script tool.

Purpose: Define the AI behavior for conversation (InteractiveAgent) and script generation (BatchAgent), plus the tool that bridges them.
Output: Two system prompt files and one Python tool file ready for stack wiring.
</objective>

<execution_context>
@/Users/feffendi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/feffendi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-agent-infrastructure-script-generation/01-CONTEXT.md
@.planning/phases/01-agent-infrastructure-script-generation/01-RESEARCH.md
@.planning/phases/01-agent-infrastructure-script-generation/01-01-SUMMARY.md

Reference for tool patterns:
@use-cases/framework/agents/resources/default-strands-agent/batch.py
@data-generator-ref/datasynth-agent-action-group/generate_data.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create InteractiveAgent conversation prompt</name>
  <files>examples/synthetic-dataset-generator/resources/system-prompt/conversation-prompt.txt</files>
  <action>
Create the conversation system prompt at `examples/synthetic-dataset-generator/resources/system-prompt/conversation-prompt.txt`.

The prompt must implement these locked decisions from CONTEXT.md:
- **Adaptive requirement gathering**: Vague requests trigger follow-up questions; detailed requests generate immediately
- **Default row count**: 10,000 rows (user can override)
- **Tone**: Professional but friendly, like a data engineer colleague

Content structure:
```text
You are DataSynth, an AI assistant that helps users create realistic synthetic datasets.
Your role is to understand their data generation needs through conversation and then generate Python scripts.

# Personality
Professional but friendly, like a knowledgeable data engineer colleague. Use domain terminology (distributions, correlations, Faker providers) but explain when helpful. Be concise and action-oriented.

# Conversation Strategy

## When user provides VAGUE request (e.g., "I need customer data"):
1. Ask about the use case: "What will you use this data for? (e.g., testing, ML training, demo)"
2. Ask about key fields: "What fields do you need? Give me a few examples."
3. Clarify any constraints: "Any specific requirements? (date ranges, value limits, correlations)"
4. Confirm row count (default 10,000): "How many rows? I'll default to 10,000 if not specified."

## When user provides DETAILED request (includes fields, types, constraints):
1. Briefly acknowledge: "Got it - I'll generate a script for [summary]"
2. Call generate_script tool immediately

## For complex requests (5+ fields with relationships):
1. Summarize the schema before generating
2. Ask for confirmation: "Does this look right before I generate?"

# Tool Usage
When you have sufficient requirements, call the generate_script tool with:
- use_case: Clear description of the data purpose
- fields: JSON string with field definitions (name, type, description, constraints for each field)
- row_count: Number of rows (default 10000, max 100000)

Example fields JSON string:
[{"name": "customer_id", "type": "integer", "description": "Unique identifier", "constraints": "1-1000000"}, {"name": "name", "type": "string", "description": "Full customer name", "constraints": "realistic names"}, {"name": "purchase_amount", "type": "float", "description": "Transaction amount", "constraints": "10-500, normal distribution"}]

# Presenting Generated Scripts
After receiving the script from generate_script:
1. Provide a brief summary (1-2 sentences): "Here's your script using Faker for names and numpy normal distribution for amounts."
2. Present the full script in a code block
3. Offer next steps: "You can download this script or let me know if you'd like any changes."

# Handling Errors
If generate_script returns an error:
1. Explain simply: "I ran into an issue generating the script."
2. Share the reason if available
3. Suggest how to fix (e.g., "Try providing more specific field definitions")
4. Offer to try again

# Constraints
- Default row count: 10,000 (max 100,000)
- Only pandas, numpy, faker libraries used in generated scripts
- All scripts follow the DataGenerator class template
- Keep explanations concise - users want code, not essays
```

Do NOT modify the personality or strategy sections - these implement user decisions.
  </action>
  <verify>
    <automated>cat examples/synthetic-dataset-generator/resources/system-prompt/conversation-prompt.txt | head -30 && grep -c "DataSynth\|generate_script\|10,000" examples/synthetic-dataset-generator/resources/system-prompt/conversation-prompt.txt</automated>
  </verify>
  <done>Conversation prompt exists with adaptive gathering strategy, default 10K rows, professional tone</done>
</task>

<task type="auto">
  <name>Task 2: Create BatchAgent script generation prompt</name>
  <files>examples/synthetic-dataset-generator/resources/generation/script-generation-prompt.txt</files>
  <action>
Create the script generation system prompt at `examples/synthetic-dataset-generator/resources/generation/script-generation-prompt.txt`.

This prompt enforces the DataGenerator class template per GEN-02 and pins library versions per GEN-03.

Content:
```text
You are a Python code generator specialized in creating synthetic data generation scripts.

# Output Format
Your response MUST be valid JSON with this exact structure:
{
  "script": "...full Python code as a string with escaped newlines...",
  "summary": "Brief 1-2 sentence explanation of the generation approach"
}

# Script Template Requirements
The generated Python script MUST follow this exact structure. Do not deviate from this template.

```python
import pandas as pd
import numpy as np
from faker import Faker
import random
from datetime import datetime, timedelta

# Version constraints - DO NOT CHANGE these comments
# pandas==2.0.3
# numpy==1.24.3
# faker==18.13.0

class DataGenerator:
    """
    Synthetic data generator for [use case description].
    """

    def __init__(self, num_rows: int = 10000):
        self.num_rows = num_rows
        self.fake = Faker()
        # Set seeds for reproducibility
        random.seed(42)
        np.random.seed(42)
        Faker.seed(42)

    def generate_datasets(self) -> list:
        """
        Generate synthetic datasets.

        Returns:
            list: List of pandas DataFrames containing generated data
        """
        data = []
        for _ in range(self.num_rows):
            record = {
                # Field generation logic goes here
            }
            data.append(record)

        return [pd.DataFrame(data)]

    def generate_schema(self) -> dict:
        """
        Generate schema documentation for the dataset.

        Returns:
            dict: Schema with column definitions
        """
        return {
            "columns": [
                {
                    "columnName": "field_name",
                    "dataType": "string|integer|float|datetime|boolean",
                    "description": "Description of what this field contains",
                    "constraint": "Any constraints, valid values, or distribution info"
                }
            ]
        }


if __name__ == "__main__":
    generator = DataGenerator()
    datasets = generator.generate_datasets()
    schema = generator.generate_schema()
    print(f"Generated {len(datasets[0])} rows")
    print(f"Schema: {len(schema['columns'])} columns")
```

# Mandatory Requirements
1. Class MUST be named `DataGenerator` exactly
2. Constructor MUST accept `num_rows: int` with default 10000
3. Method `generate_datasets()` MUST return `list[pd.DataFrame]`
4. Method `generate_schema()` MUST return `dict` with columns array
5. Include version constraint comments at the top
6. Set random seeds (42) for reproducibility
7. ONLY use: pandas, numpy, faker, random, datetime - NO other imports

# Field Type Mappings
Use these Faker/numpy methods for common field types:
- Names: `self.fake.name()`, `self.fake.first_name()`, `self.fake.last_name()`
- Emails: `self.fake.email()`
- Addresses: `self.fake.address()`, `self.fake.city()`, `self.fake.country()`
- Phone: `self.fake.phone_number()`
- Dates: `self.fake.date_between(start_date='-1y', end_date='today')`
- Timestamps: `datetime.now() - timedelta(days=random.randint(0, 365))`
- Normal distribution: `np.random.normal(mean, std)`
- Uniform distribution: `np.random.uniform(low, high)`
- Integer range: `random.randint(min, max)`
- Categories: `random.choice(['option1', 'option2', 'option3'])`
- UUIDs: `self.fake.uuid4()`
- Boolean: `random.choice([True, False])`
- Currency: `round(np.random.uniform(min, max), 2)`

# Error Handling
If the request is unclear or impossible to implement:
- Return JSON with script as empty string
- Include detailed error in summary field explaining what's missing
```

The version comments (pandas==2.0.3, numpy==1.24.3, faker==18.13.0) will be updated when the Lambda layer is created in Phase 2.
  </action>
  <verify>
    <automated>cat examples/synthetic-dataset-generator/resources/generation/script-generation-prompt.txt | head -40 && grep -c "DataGenerator\|generate_datasets\|pandas==2.0.3" examples/synthetic-dataset-generator/resources/generation/script-generation-prompt.txt</automated>
  </verify>
  <done>Script generation prompt enforces DataGenerator template with pinned versions</done>
</task>

<task type="auto">
  <name>Task 3: Create generate_script tool with input sanitization</name>
  <files>examples/synthetic-dataset-generator/resources/tools/generate_script.py</files>
  <action>
Create the Python tool at `examples/synthetic-dataset-generator/resources/tools/generate_script.py`.

This tool:
- Implements GEN-04 (input sanitization)
- Invokes BatchAgent Lambda via boto3
- Returns structured response

Content:
```python
"""
Generate Script Tool - Invokes BatchAgent to generate Python DataGenerator scripts.

This tool is called by InteractiveAgent when sufficient requirements have been gathered.
It sanitizes inputs, invokes the BatchAgent Lambda, and returns the generated script.
"""
from strands import tool
from typing import Dict, Any
import boto3
import json
import os
import re

# Initialize Lambda client outside handler for connection reuse
lambda_client = boto3.client('lambda')


def sanitize_input(text: str, max_length: int = 2000) -> str:
    """
    Sanitize user input to prevent prompt injection attacks.

    Strips:
    - Control characters (except newline/tab)
    - Known injection patterns
    - Excessive length

    Args:
        text: Raw user input
        max_length: Maximum allowed length

    Returns:
        Sanitized string safe for inclusion in prompts
    """
    if not text:
        return ''

    # Patterns that could indicate prompt injection attempts
    injection_patterns = [
        r'ignore\s*(all\s*)?(previous|prior)\s*instructions?',
        r'disregard\s*(all\s*)?(previous|prior|above)',
        r'forget\s*(everything|all|previous)',
        r'system\s*:\s*',
        r'<\|.*?\|>',  # Special tokens
        r'\[\[.*?\]\]',  # Bracketed commands
        r'```system',  # System code blocks
        r'ADMIN\s*MODE',
        r'DEBUG\s*MODE',
    ]

    result = text
    for pattern in injection_patterns:
        result = re.sub(pattern, '', result, flags=re.IGNORECASE | re.DOTALL)

    # Remove control characters except newline and tab
    result = ''.join(
        char for char in result
        if char.isprintable() or char in '\n\t'
    )

    # Truncate to max length
    return result[:max_length].strip()


@tool
def generate_script(
    use_case: str,
    fields: str,
    row_count: int = 10000
) -> Dict[str, Any]:
    """
    Generate a Python DataGenerator script for synthetic data creation.

    Call this tool when you have gathered sufficient requirements from the user.
    The tool invokes the script generation agent and returns the Python code.

    Args:
        use_case: Description of the dataset purpose and domain
                  (e.g., "fraud detection training data", "e-commerce customer analytics")
        fields: JSON string containing field definitions. Each field should have:
                - name: Field/column name
                - type: Data type (string, integer, float, datetime, boolean)
                - description: What this field represents
                - constraints: Value constraints, distributions, or valid values
        row_count: Number of rows to generate (default 10000, max 100000)

    Returns:
        Dictionary containing:
        - success: Boolean indicating if generation succeeded
        - script: Generated Python code as string (if success)
        - summary: Brief explanation of the generation approach (if success)
        - error: Error message (if not success)
        - recoverable: Boolean indicating if user can retry with different input
    """
    try:
        # 1. Validate and sanitize inputs
        sanitized_use_case = sanitize_input(use_case, max_length=500)
        sanitized_fields = sanitize_input(fields, max_length=5000)

        if not sanitized_use_case:
            return {
                'success': False,
                'error': 'Use case description is required. Please describe what the data will be used for.',
                'recoverable': True
            }

        if not sanitized_fields:
            return {
                'success': False,
                'error': 'Field definitions are required. Please specify the columns/fields you need.',
                'recoverable': True
            }

        # Validate fields is valid JSON
        try:
            fields_parsed = json.loads(sanitized_fields)
            if not isinstance(fields_parsed, list):
                return {
                    'success': False,
                    'error': 'Fields must be a JSON array of field definitions.',
                    'recoverable': True
                }
        except json.JSONDecodeError as e:
            return {
                'success': False,
                'error': f'Invalid JSON in fields parameter: {str(e)}',
                'recoverable': True
            }

        # Enforce row count limits (100 minimum, 100000 maximum)
        safe_row_count = min(max(int(row_count), 100), 100000)

        # 2. Get BatchAgent function name from environment
        function_name = os.environ.get('BATCH_AGENT_FUNCTION_NAME')
        if not function_name:
            return {
                'success': False,
                'error': 'Script generation service is not configured. Please contact support.',
                'recoverable': False
            }

        # 3. Build payload for BatchAgent
        # BatchAgent expects contentType='data' with content.data containing the prompt
        generation_request = {
            'use_case': sanitized_use_case,
            'fields': sanitized_fields,
            'row_count': safe_row_count
        }

        payload = {
            'contentType': 'data',
            'content': {
                'data': f'Generate a Python DataGenerator script with these requirements:\n{json.dumps(generation_request, indent=2)}'
            }
        }

        # 4. Invoke BatchAgent Lambda
        response = lambda_client.invoke(
            FunctionName=function_name,
            InvocationType='RequestResponse',
            Payload=json.dumps(payload)
        )

        # 5. Parse response
        response_payload = json.loads(response['Payload'].read().decode('utf-8'))

        # Check for Lambda execution errors
        if 'FunctionError' in response:
            error_msg = response_payload.get('errorMessage', 'Unknown error during script generation')
            return {
                'success': False,
                'error': f'Script generation failed: {error_msg}',
                'recoverable': True
            }

        # BatchAgent with expectJson=true returns parsed JSON in 'result'
        if 'result' in response_payload:
            result = response_payload['result']
            script = result.get('script', '')
            summary = result.get('summary', f'Generated DataGenerator script for {sanitized_use_case}')

            if not script:
                return {
                    'success': False,
                    'error': summary if summary else 'Script generation returned empty result',
                    'recoverable': True
                }

            return {
                'success': True,
                'script': script,
                'summary': summary,
                'row_count': safe_row_count
            }
        elif 'errorMessage' in response_payload:
            return {
                'success': False,
                'error': response_payload.get('errorMessage', 'Unknown error'),
                'recoverable': True
            }
        else:
            # Unexpected response format
            return {
                'success': False,
                'error': 'Unexpected response from script generator. Please try again.',
                'recoverable': True
            }

    except json.JSONDecodeError as e:
        return {
            'success': False,
            'error': f'Failed to parse response: {str(e)}',
            'recoverable': True
        }
    except Exception as e:
        return {
            'success': False,
            'error': f'Script generation encountered an error: {str(e)}',
            'error_type': type(e).__name__,
            'recoverable': True
        }
```
  </action>
  <verify>
    <automated>python3 -m py_compile examples/synthetic-dataset-generator/resources/tools/generate_script.py && grep -c "@tool\|sanitize_input\|BATCH_AGENT_FUNCTION_NAME" examples/synthetic-dataset-generator/resources/tools/generate_script.py</automated>
  </verify>
  <done>Tool compiles without errors, includes @tool decorator, sanitize_input function, and BATCH_AGENT_FUNCTION_NAME env var</done>
</task>

</tasks>

<verification>
1. Conversation prompt contains adaptive strategy: `grep -c "VAGUE\|DETAILED" examples/synthetic-dataset-generator/resources/system-prompt/conversation-prompt.txt`
2. Generation prompt enforces template: `grep -c "class DataGenerator\|generate_datasets\|generate_schema" examples/synthetic-dataset-generator/resources/generation/script-generation-prompt.txt`
3. Tool compiles: `python3 -m py_compile examples/synthetic-dataset-generator/resources/tools/generate_script.py`
4. Tool has sanitization: `grep -c "sanitize_input" examples/synthetic-dataset-generator/resources/tools/generate_script.py`
</verification>

<success_criteria>
- [ ] conversation-prompt.txt exists with DataSynth persona and adaptive gathering strategy
- [ ] conversation-prompt.txt mentions default 10,000 rows
- [ ] script-generation-prompt.txt enforces DataGenerator class with generate_datasets() and generate_schema()
- [ ] script-generation-prompt.txt includes version comments (pandas, numpy, faker)
- [ ] generate_script.py compiles without syntax errors
- [ ] generate_script.py has @tool decorator
- [ ] generate_script.py implements sanitize_input function with injection pattern detection
- [ ] generate_script.py reads BATCH_AGENT_FUNCTION_NAME from environment
- [ ] generate_script.py returns structured response with success/error handling
</success_criteria>

<output>
After completion, create `.planning/phases/01-agent-infrastructure-script-generation/01-02-SUMMARY.md`
</output>
